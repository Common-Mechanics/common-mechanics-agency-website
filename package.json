{
  "name": "common-mechanics-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "predev": "npm run dpa-manifest && npm run check-privacy",
    "dev": "astro dev",
    "prebuild": "npm run dpa-manifest && npm run check-privacy",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "capture-previews": "node scripts/capture-previews.mjs",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy",
    "dpa-manifest": "node scripts/data-processing-manifest.mjs",
    "check-privacy": "node scripts/check-privacy-claims.mjs",
    "fetch-dpas": "node scripts/fetch-vendor-dpas.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.6.0",
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource-variable/dm-sans": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "astro": "^6.0.8"
  },
  "devDependencies": {
    "puppeteer": "^24.40.0",
    "wrangler": "^4.95.0"
  }
}
