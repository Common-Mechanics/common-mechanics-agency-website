---
/**
 * One version of the sub-processor list, rendered as a record rather than a
 * bare list of names.
 *
 * Every entry carries the same four fields, and only three of them appear
 * here. `registeredAddress` is maintained but not rendered: EDPB Opinion
 * 22/2024 requires a controller to be able to have the name, address and
 * contact person of every sub-processor readily available, not to find them
 * published — so the page offers the address on request and the JSON is what
 * answers. See src/data/subprocessors/README.md for what each field is for.
 *
 * The `<dl>` is deliberate: a screen reader announces the term with the value,
 * so "Processed in, United States, European Union" reads as one fact. A table
 * would be the other reasonable choice, but a table does not survive a 320px
 * viewport, and this page is required reading, not optional.
 */
import type { Subprocessor } from '../data/data-processing';

interface Props {
  subprocessors: Subprocessor[];
}

const { subprocessors } = Astro.props;
---

<ul class="subprocessors">
  {subprocessors.map((subprocessor) => (
    <li class="subprocessor">
      <p class="subprocessor-head">
        <strong>{subprocessor.name}</strong> &ndash; {subprocessor.function}
      </p>

      <dl class="subprocessor-detail">
        <dt>Entity</dt>
        <dd>{subprocessor.legalEntity}</dd>

        <dt>Processed in</dt>
        <dd>{subprocessor.processingLocations.join(', ')}</dd>
      </dl>
    </li>
  ))}
</ul>

<style>
  .subprocessors {
    list-style: none;
    margin-top: var(--space-4);
  }

  .subprocessor {
    position: relative;
    padding-left: var(--space-6);
    max-width: 68ch;
    /* Long entity names must not widen the page at 320px, or when a
       low-vision visitor has raised their root font size. */
    overflow-wrap: anywhere;
  }

  .subprocessor + .subprocessor {
    margin-top: var(--space-6);
  }

  /* Technical-drawing dash instead of a bullet — matches .dpa-body li */
  .subprocessor::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.75em;
    width: var(--space-3);
    height: 1px;
    background: var(--color-rule);
  }

  .subprocessor-head {
    font-family: var(--font-sans);
    font-size: var(--font-size-base);
    line-height: 1.7;
  }

  .subprocessor-head strong {
    font-weight: 700;
  }

  /* ─── Detail rows ──────────────────────────────── */
  .subprocessor-detail {
    display: grid;
    /* The label column sizes to the longest term, then stops growing */
    grid-template-columns: minmax(0, max-content) minmax(0, 1fr);
    gap: var(--space-1) var(--space-4);
    margin-top: var(--space-2);
    padding-left: var(--space-3);
    border-left: 1px solid var(--color-accent-line);
  }

  .subprocessor-detail dt {
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    letter-spacing: var(--tracking-wide);
    color: var(--color-muted);
    line-height: 1.6;
    /* Terms read as labels, not as sentence text */
    text-transform: lowercase;
  }

  .subprocessor-detail dd {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    line-height: 1.6;
    margin: 0;
  }

  /* Under ~34rem the two columns stop fitting an address; stack them so the
     value gets the full measure rather than breaking mid-word in a 12ch cell. */
  @media (max-width: 34rem) {
    .subprocessor-detail {
      grid-template-columns: minmax(0, 1fr);
      gap: 0;
    }

    .subprocessor-detail dt {
      margin-top: var(--space-2);
    }

    .subprocessor-detail dt:first-of-type {
      margin-top: 0;
    }
  }
</style>
