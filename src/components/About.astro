<section class="section" data-rule="bottom">
  <span class="section-label" data-animate style="--stagger: 2">About</span>
  <p class="about-body" data-animate style="--stagger: 3">
    We're a design-build studio, crafting design systems and digital products for the world's
    most pressing causes. We partner with organisations to bring their product to life, from concept
    through to launch and maintenance.
  </p>
  <p class="about-founded" data-animate style="--stagger: 4">Founded 2026</p>
</section>

<style>
  .about-body {
    font-family: var(--font-sans);
    /* Key copy — shrinks smoothly from xl to lg as the viewport narrows */
    font-size: clamp(var(--font-size-lg), 3.2vw, var(--font-size-xl));
    color: var(--color-fg);
    max-width: 56ch;
    line-height: 1.5;
    margin-bottom: var(--space-4);
  }

  .about-founded {
    font-size: var(--font-size-xs);
    color: var(--color-muted);
    letter-spacing: var(--tracking-wide);
  }
</style>
