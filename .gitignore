node_modules/
dist/
.astro/
.env
.env.*
!.env.example

# regenerated by `npm run dpa-manifest` on every dev/build (see scripts/)
src/data/data-processing.generated.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example

# per-developer Claude Code overrides; team-shared config lives in settings.json
.claude/settings.local.json
local-claude-files/
